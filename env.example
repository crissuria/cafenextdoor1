# Flask Secret Key
# Generate a secure random key: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here-change-this-in-production

# Flask Environment
FLASK_ENV=production
FLASK_DEBUG=False

# Database Configuration
DATABASE_PATH=database/cafe.db

# Email Configuration (ProtonMail Bridge)
# ProtonMail Bridge runs locally and provides SMTP access
# Download from: https://proton.me/mail/bridge
# Make sure Bridge is running before starting the app
MAIL_SERVER=127.0.0.1
MAIL_PORT=1025
MAIL_USE_TLS=True
MAIL_USERNAME=cafenextdoor@protonmail.com
MAIL_PASSWORD=your-protonmail-bridge-password
MAIL_DEFAULT_SENDER=cafenextdoor@protonmail.com
CAFE_EMAIL=cafenextdoor@protonmail.com

# Alternative: Direct SMTP (For production/deployment without Bridge)
# Uncomment these if using direct SMTP instead of Bridge:
# MAIL_SERVER=mail.protonmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=cafenextdoor@protonmail.com
# MAIL_PASSWORD=your-protonmail-password

# Production Server
HOST=0.0.0.0
PORT=5000

# Security
# Set to True in production to enforce HTTPS
FORCE_HTTPS=False


















